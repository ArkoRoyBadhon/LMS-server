import { Router } from 'express'
// import authController from '../controllers/auth.controller'
// import checker from '../middlewares/authMiddleware'
const router = Router()

// router.post('/login', authController.login)
// router.post(
//   '/c/admin',
//   checker.isAuthenticated,
//   checker.roleChecker(['SUPER_ADMIN']),
//   authController.adminCreation,
// )
// router.post('/c/teacher', authController.createTeacher)
// router.post('/c/student', authController.createStudent)
// router.post('/logout', authController.logout)

export default router
